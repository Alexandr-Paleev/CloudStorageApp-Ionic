import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cloudstorage.app',
  appName: 'Cloud Storage',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;












